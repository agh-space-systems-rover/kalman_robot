name: PR Format Check

on:
  workflow_dispatch:
  pull_request:
  push:

jobs:
  call-parent-format-check:
    uses: agh-space-systems-rover/kalman-ws/.github/workflows/format-check.yml@feat/add-code-styling-check-workflow
    with:
      paths: .
